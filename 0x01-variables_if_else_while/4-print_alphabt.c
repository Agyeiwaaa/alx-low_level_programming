#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - C program that prints alphabets in both upper and lower cases except q and e
*
* Return: 0 (Success)
*/
int main(void)
{
int i;
for (i = 97; i < 123; i++);
}
if (i != 101 && i!= 113);
{
putchar(i);
}
putchar('\n');
return (0);
}
