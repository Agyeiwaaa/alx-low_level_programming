#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - C program that prints alphabets in both upper and lower cases except q and e
*
* Return: 0 (Success)
*/
int main(void)
{
int x;

for (x = 65; x < ; x++)
{
if (x != 65 && x != 90)
{
putchar(x);
}
}
putchar('\n');
return (0);
}
