#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
*main - Entry point
*Description:'print numbers' 
*Return: 0 (success)
*/
int main(void)
{
int x, y;

for (x = '0'; y < '9'; x++)
{

for (y = x + 1; y <='9'; y++)
{
if (e != x)
{
putchar(x);
putchar(y);
if (x == '8' && y == '9')
}
putchar(',');
putchar(' ');
{
puttchar('\n');
return (0);
}
