#include <stdio.h>
/**
 * main - C program that prints a message with prinf function
 * Return: 0 (Success)
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
printf("with proper grammar, but the outcome is a piece of art,\n");		
return (0);
}
